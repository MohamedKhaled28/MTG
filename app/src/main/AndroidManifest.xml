<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.mody_task">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mtg"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UserActivity" />
        <activity android:name=".PlaceActivity" />
        <activity android:name=".ArtifactdescActivity" />
        <activity android:name=".Install_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home_activity" />
        <activity android:name=".PlacedetailsActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".Change_activity" />
        <activity android:name=".ViewScanningDetailsActivity" />
        <activity android:name=".HelpActivity" />

        <receiver android:name=".Checkreceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <activity android:name=".Allartifacts"></activity>
    </application>

</manifest>